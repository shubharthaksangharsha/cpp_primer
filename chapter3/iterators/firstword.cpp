#include<iostream>
#include<vector>
using std::cout;
using std::cin;
using std::endl;
using std::string;
using std::vector;

int main(){
  string word;
  vector<string> svec();
  while(push_back(word));
  for(auto first = word.begin() ; first !=word.end() && !isspace(*first) ; ++first){
    *first = toupper(*first);


  }
  cout<<word<<endl;

  return 0;
}
    
  
