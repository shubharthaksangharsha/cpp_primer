//binary search
#include<iostream>
#include<vector>
#include<string>
using std::cout;
using std::endl;
using std::string;
using std::vector;

int main(){
  vector<string> ivec{"a", "b" , "c" , "d", "e" , "f" , "g"};

  vector<string>::iterator f = ivec.begin();
  vector<string>::iterator l = ivec.end();
  vector<string>::iterator m = f + (l-f)/2;

  while((m!= l) && (*m) ! = "a")){
    if(s<*m)
      e = m ;
    else
      f = l+1;
    m = f + (l-f)/2;
    


  return 0;
}
    
